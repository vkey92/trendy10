name: Android CI

on:
  push:
    branches: [ version-1.0-git-actions ]
  pull_request:
    branches: [ version-1.0-git-actions ]

jobs:
  build:

    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Gradle
        run: ./gradlew build
